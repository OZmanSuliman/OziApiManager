# OziApiManager

A description of this package.


##USAGE

    let api = ApiManager()
        api.getMethod(url: "") {  response, error, statusCode in
            // handle success
        } withapiFiluer: { error in
            // handle error
        }
